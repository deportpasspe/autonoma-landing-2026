---
import type { HeroSection } from "../../lib/cms/types";
import Container from "../../components/ui/Container.astro";

interface Props extends HeroSection {
  formRail?: boolean;
}

const { line1, line2, line3, line4, imageSrc, imageAlt, formRail = false } = Astro.props as Props;
---

<section
  id="inicio"
  class="relative overflow-hidden bg-brand-accessible pt-[95px] text-white"
  aria-labelledby="hero-title"
>
  <Container
    formRail={formRail}
    class="relative pb-6 pt-6 md:pb-8 md:pt-8 lg:pb-[var(--space-section-y)] lg:pt-12"
  >
    <div class="relative w-full" data-reveal>
      <div class="@container relative w-full overflow-hidden rounded-[var(--radius-card)] lg:rounded-none lg:overflow-visible">
        <img
          src={imageSrc}
          alt={imageAlt}
          class="block h-auto w-full"
          width="856"
          height="527"
          loading="eager"
          fetchpriority="high"
          decoding="async"
        />
        <h1
          id="hero-title"
          class="absolute left-[8%] top-[16%] z-10 flex w-[42%] min-w-[9rem] max-w-[min(16.75rem,46%)] flex-col leading-[1.1] sm:left-[11.4%] sm:top-[18.1%] sm:w-[30%] sm:max-w-[min(16.75rem,28%)]"
        >
          <span class="text-[length:clamp(1.75rem,11.75cqw,7.5rem)] font-bold text-white">
            {line1}
          </span>
          <span class="text-[length:clamp(1rem,6.3cqw,4rem)] font-bold text-white">
            {line2}
          </span>
          <span class="text-[length:clamp(1rem,6.3cqw,4rem)] font-semibold text-brand">
            {line3}
          </span>
          <span class="text-[length:clamp(1.25rem,8.55cqw,5.5rem)] font-bold text-brand">
            {line4}
          </span>
        </h1>
      </div>
    </div>
  </Container>
</section>
